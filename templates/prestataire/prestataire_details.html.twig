{% extends 'base.html.twig' %}
{% block body %}
    {% block container %}
        <div class="container">
            <div class="row">
                <div class="col-md-10">
                    <h4>{{ prestataire.name }}</h4>
                    <ul>
                        {% for prest in prestataires %}
                            <li><a href="{{ path('prestataire_show', {'id':prest.id}) }}">
                                    {{ prestataire.localite }} )
                                </a> </li>
                        {% endfor %}
                    </ul>
                </div>
            </div>
        </div>
    {% endblock %}
{% endblock %}